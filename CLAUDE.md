All project instructions, conventions, and knowledge live in `AGENTS.md` and
the `.brain/` topic files. Read `AGENTS.md` first — it indexes:

- `.brain/architecture.md` — crates, container tree, event flow, state machines
- `.brain/conventions.md` — code style, error handling, cfg patterns, pitfalls
- `.brain/environment.md` — toolchain, build/test/lint commands, known failures
- `.brain/worklog.md` — change history + upstream issue knowledge base

Do not duplicate content here; update `AGENTS.md`/`.brain/` instead.
